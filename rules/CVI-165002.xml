<?xml version="1.0" encoding="UTF-8"?>
<cobra document="https://github.com/WhaleShark-Team/cobra">
    <name value="LDAP注入"/>
    <language value="java"/>
    <match mode="function-param-controllable"><![CDATA[(context.search)]]></match>
    <level value="5"/>
    <test>
        <case assert="true"><![CDATA[
           NamingEnumeration<SearchResult> answers = context.search("dc=People,dc=example,dc=com", "(uid=" + username + ")", ctrls);
       ]]></case>
    </test>
    <solution>
        ## 安全风险

        LDAP Injection
        允许进行LDAP查询 + 输入未进行过滤 ---> LDAP注入
        这种威胁可以让攻击者能够从LADP树中提取到很多很重要的信息

        ## 修复方案
        对用户输入数据中包含的”语言本身的保留字符”进行转义

    </solution>
    <status value="on"/>
    <author name="BlBana" email="blbana@qq.com"/>
</cobra>

