<?xml version="1.0" encoding="UTF-8"?>
<cobra document="https://github.com/wufeifei/cobra">
    <name value="echo或print直接输出入参导致XSS"/>
    <language value="php"/>
    <match mode="regex-param-controllable"><![CDATA[(echo|print)\s*(\()?\s*.*\s*(,\s*.*)*(\))?;]]></match>
    <repair block="in-function"><![CDATA[(htmlspecialchars\s*\(\s*{{PARAM}}\s*)]]></repair>
    <level value="4"/>
    <test>
        <case assert="true"><![CDATA[echo ($_GET['test']);]]></case>
        <case assert="true"><![CDATA[echo $_GET['test'];]]></case>
        <case assert="true"><![CDATA[print("Hello " . $_GET["name"]);]]></case>
        <case assert="true"><![CDATA[print 'foo is $_GET['test']';]]></case>
    </test>
    <solution>
        ## 安全风险
        直接输出入参会导致XSS

        ## 修复方案
        1. 使用`htmlentities`函数进行转义
            ```php
            print("Hello " . htmlentities($_GET["name"], ENT_QUOTES, "utf-8");
            ```
        2. 使用Begis安全组件对参数进行过滤后使用
    </solution>
    <status value="on"/>
    <author name="Feei" email="feei@feei.cn"/>
</cobra>